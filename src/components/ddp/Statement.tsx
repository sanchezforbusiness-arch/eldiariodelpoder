export function Statement() {
  return (
    <section className="py-24 md:py-40 bg-background border-t border-white/5">
      <div className="container-ddp">
        <p className="eyebrow mb-8">La declaración</p>
        <p className="font-display font-medium uppercase leading-[0.95] tracking-tight text-foreground text-[clamp(1.75rem,4.5vw,4rem)] max-w-6xl">
          Conversaciones sin filtro con las personas que han definido la
          política, la empresa y la cultura.{" "}
          <span className="text-foreground/40">
            Los mayores referentes dejan su legado a la nueva generación.
          </span>
        </p>
      </div>
    </section>
  );
}
